[package]
name = "rust-benchmark"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
glob = "0.3"
chrono = { version = "0.4", features = ["serde"] }
rayon = "1.10"
